{
  "name": "agyloop",
  "version": "0.1.0",
  "description": "Multi-Subagent Development Lifecycle Orchestrator for Google Antigravity",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "agyloop": "./bin/agyloop.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc -p tsconfig.test.json --noEmit",
    "test": "npm run build && node --test tests/*.test.ts tests/**/*.test.ts",
    "test:coverage": "npm run build && node --test --experimental-test-coverage tests/*.test.ts tests/**/*.test.ts",
    "prepack": "npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "Alvin Jorrel Pascual <me@ajx.codes> (https://github.com/ajxcodes)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ajxcodes/agyloop.git"
  },
  "keywords": [
    "antigravity",
    "agy",
    "orchestrator",
    "subagents",
    "pair-programming",
    "lifecycle",
    "state-machine"
  ],
  "devDependencies": {
    "@types/node": "^22.20.2",
    "typescript": "^7.0.2"
  }
}
